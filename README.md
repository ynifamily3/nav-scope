# nav-scope

A library for adding nestable semantic navigation scopes on top of the browser's linear history.

It groups navigation flows such as dialogs, wizards, and drawers into scopes, letting you navigate within a flow or exit it as a whole.
